//
// Created by Hà Tường Nguyên on 9/13/23.
//

#include "LecturePackage.h"

void Lec10X(){
    std::cout << "Lecture 10X: ESCAPE CHARACTERS\n" << std::endl;

    cout << "'\\n'" << endl;
    cout << "\tNewline: Represents a newline character, moving the cursor or output to the beginning of the next line." << endl << endl;

    cout << "'\\r'" << endl;
    cout << "\tCarriage Return: Represents a carriage return character, moving the cursor or output to the beginning of the current line." << endl << endl;

    cout << "'\\t'" << endl;
    cout << "\tTab: Represents a tab character, inserting horizontal whitespace for indentation or formatting." << endl << endl;

    cout << "'\\b'" << endl;
    cout << "\tBackspace: Represents a backspace character, moving the cursor or output back one position." << endl << endl;

    cout << "'\\'" << endl;
    cout << "\tBackslash: Represents a literal backslash character when you want to include it in a string." << endl << endl;

    cout << "'\''" << endl;
    cout << "\tSingle Quote: Represents a literal single quote character within a single-quoted character literal." << endl << endl;

    cout << "'\"'" << endl;
    cout << "\tDouble Quote: Represents a literal double quote character within a double-quoted string literal." << endl << endl;

    cout << "'\\a'" << endl;
    cout << "\tAlert or Bell: Represents an audible or visual alert, often producing a beep sound or flashing the console." << endl << endl;

    cout << "'\\f'" << endl;
    cout << "\tForm Feed: Represents a form feed character, which is used for page breaks or similar formatting in some contexts." << endl << endl;

    cout << "'\\v'" << endl;
    cout << "\tVertical Tab: Represents a vertical tab character, used for vertical spacing in some contexts." << endl << endl;
}